#!/usr/bin/env bash

if [ -z "$SKILLSEED_LOCATION" ]; then
    export SKILLSEED_LOCATION="$HOME/mantro/projects/skillseed";
fi
alias skillseed='cd $SKILLSEED_LOCATION';
alias sss='cd $SKILLSEED_LOCATION/server';
alias ssf='cd $SKILLSEED_LOCATION/frontend';

