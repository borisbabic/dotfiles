# Application Launchers
bind=SUPER_L,SUPER_L,spawn,dms ipc call spotlight toggle
bind=NONE,XF86Assistant,spawn,dms ipc call spotlight toggle
# the above doesn't work :sadge:
bind=SUPER,grave,spawn,dms ipc call spotlight toggle
bind=SUPER,escape,spawn,dms ipc call spotlight toggle
bind=SUPER,space,spawn,dms ipc call spotlight toggle
bind=SUPER,v,spawn,dms ipc call clipboard toggle
bind=SUPER,p,spawn,dms ipc call processlist focusOrToggle
bind=SUPER,comma,spawn,dms ipc call settings focusOrToggle
bind=SUPER,n,spawn,dms ipc call notifications toggle
bind=SUPER,y,spawn,dms ipc call dankdash wallpaper
bind=SUPER,w,spawn,dms ipc call widget toggle weather

# Security
bind=SUPER+ALT,l,spawn,dms ipc call lock lock

# Audio Controls
bind=NONE,XF86AudioRaiseVolume,spawn,dms ipc call audio increment 3
bind=NONE,XF86AudioLowerVolume,spawn,dms ipc call audio decrement 3
bind=NONE,XF86AudioMute,spawn,dms ipc call audio mute

# Brightness Controls
bind=NONE,XF86MonBrightnessUp,spawn,dms ipc call brightness increment 5 backlight:intel_backlight
bind=NONE,XF86MonBrightnessDown,spawn,dms ipc call brightness decrement 5 backlight:intel_backlight

bind=NONE,PRINT,spawn, dms screenshot
bind=SUPER,PRINT,spawn, dms screenshot window
bind=CTRL,PRINT,spawn, dms screenshot full
bind=CTRL+SHIFT,PRINT,spawn, dms screenshot all
